const {resolve} = require('path');
const fs = require('fs');

***REMOVED***
  devtool: 'none',
  mode: 'production',
  entry: {
    index: [__dirname + '/lib/index.js']
  },
  output: {
    path: __dirname,
    library: 'sift',
    libraryTarget: 'umd',
    filename: 'sift.min.js'
  },
  resolve: {
    extensions: ['.js']
  },
  module: {
    rules: [
    ]
  }
***REMOVED***