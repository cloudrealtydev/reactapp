"use strict";

var isValue = require("./is-value");

var map = { function: true, object: true ***REMOVED***

module.exports = function (value) { return (isValue(value) && map[typeof value]) || false; ***REMOVED***
