'use strict';

var $isNaN = Number.isNaN || function (a) { return a !== a; ***REMOVED***

module.exports = Number.isFinite || function (x) { return typeof x === 'number' && !$isNaN(x) && x !== Infinity && x !== -Infinity; ***REMOVED***
