# Command-Option-Argument
[![build status](https://secure.travis-ci.org/veged/coa.png)](http://travis-ci.org/veged/coa)

## Что �?то?

COA — пар�?ер параметров командной �?троки, позвол�?ющий извлечь мак�?имум пользы от формального API вашей программы.
Как только вы опишете определение в терминах команд, параметров и аргументов, вы автоматиче�?ки получите:

* Справку дл�? командной �?троки
* API дл�? и�?пользовани�? программы как модул�? в COA-�?овме�?тимых программах
* �?втодополнение дл�? командной �?троки

### Прочие возможно�?ти

* Широкий выбор на�?троек дл�? параметров и аргументов, включа�? множе�?твенные значени�?, логиче�?кие значени�? и об�?зательно�?ть параметров
* Возможно�?ть а�?инхронного и�?полнени�? команд, и�?пользу�? проми�?ы (и�?пользует�?�? библиотека [Q](https://github.com/kriskowal/q))
* Про�?тота и�?пользовани�? �?уще�?твующих команд как подмодулей дл�? новых команд
* Комбинированна�? валидаци�? и анализ �?ложных значений

## Примеры

````javascript
require('coa').Cmd() // деклараци�? команды верхнего уровн�?
    .name(process.argv[1]) // им�? команды верхнего уровн�?, берем из имени программы
    .title('Жутко полезна�? утилита дл�? командной �?троки') // название дл�? и�?пользовани�? в �?правке и �?ообщени�?х
    .helpful() // добавл�?ем поддержку �?правки командной �?троки (-h, --help)
    .opt() // добавл�?ем параметр
        .name('version') // им�? параметра дл�? и�?пользовани�? в API
        .title('Version') // тек�?т дл�? вывода в �?ообщени�?х
        .short('v') // короткое им�? параметра: -v
        .long('version') // длинное им�? параметра: --version
        .flag() // параметр не требует ввода значени�?
        .act(function(opts) {  // дей�?тви�? при вызове аргумента
            // результатом �?вл�?ет�?�? вывод тек�?тового �?ообщени�?
            return JSON.parse(require('fs').readFileSync(__dirname + '/package.json'))
                .version;
        })
        .end() // завершаем определение параметра и возвращаем�?�? к определению верхнего уровн�?
    .cmd().name('subcommand').apply(require('./subcommand').COA).end() // загрузка подкоманды из модул�?
    .cmd() // добавл�?ем еще одну подкоманду
        .name('othercommand').title('Еще одна полезна�? подпрограмма').helpful()
        .opt()
            .name('input').title('input file, required')
            .short('i').long('input')
            .val(function(v) { // функци�?-валидатор, также может и�?пользовать�?�? дл�? тран�?формации значений параметров
                return require('fs').createReadStream(v) })
            .req() // параметр �?вл�?ет�?�? об�?зательным
            .end() // завершаем определение параметра и возвращаем�?�? к определению команды
        .end() // завершаем определение подкоманды и возвращаем�?�? к определению команды верхнего уровн�?
    .run(process.argv.slice(2)); // разбираем process.argv и запу�?каем
````

````javascript
// subcommand.js
exports.COA = function() {
    this
        .title('Полезна�? подпрограмма').helpful()
        .opt()
            .name('output').title('output file')
            .short('o').long('output')
            .output() // и�?пользовать �?тандартную на�?тройку дл�? параметра вывода
            .end()
***REMOVED***
````

## API

### Cmd
Команда — �?ущно�?ть верхнего уровн�?. У команды могут быть определены параметры и аргументы.

#### Cmd.api
Возвращает объект, который можно и�?пользовать в других программах. Подкоманды �?вл�?ют�?�? методами �?того объекта.<br>
**@returns** *{Object}*

#### Cmd.name
Определ�?ет канониче�?кий идентификатор команды, и�?пользуемый в вызовах API.<br>
**@param** *String* `_name` им�? команды<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.title
Определ�?ет название команды, и�?пользуемый в тек�?товых �?ообщени�?х.<br>
**@param** *String* `_title` название команды<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.cmd
Создает новую подкоманду или добавл�?ет ранее определенную подкоманду к текущей команде.<br>
**@param** *COA.Cmd* `[cmd]` �?кземпл�?р ранее определенной подкоманды<br>
**@returns** *COA.Cmd* �?кземпл�?р новой или ранее определенной подкоманды

#### Cmd.opt
Создает параметр дл�? текущей команды.<br>
**@returns** *COA.Opt* `new` �?кземпл�?р параметра

#### Cmd.arg
Создает аргумент дл�? текущей команды.<br>
**@returns** *COA.Opt* `new` �?кземпл�?р аргумента

#### Cmd.act
Добавл�?ет (или �?оздает) дей�?твие дл�? текущей команды.<br>
**@param** *Function* `act` функци�?,
    выполн�?ема�? в контек�?те �?кземпл�?ра текущей команды
    и принимающа�? �?ледующие параметры:<br>
        - *Object* `opts` параметры команды<br>
        - *Array* `args` аргументы команды<br>
        - *Object* `res` объект-аккумул�?тор результатов<br>
    Функци�? может вернуть проваленный проми�? из Cmd.reject (в �?лучае ошибки)
    или любое другое значение, ра�?�?матриваемое как результат.<br>
**@param** *{Boolean}* [force=false] флаг, назначающий немедленное и�?полнение вме�?то добавлени�? к �?пи�?ку �?уще�?твующих дей�?твий<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.apply
И�?полн�?ет функцию �? переданными аргументами в контек�?те �?кземпл�?ра текущей команды.<br>
**@param** *Function* `fn`<br>
**@param** *Array* `args`<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.comp
�?азначает ка�?томную функцию автодополнени�? дл�? текущей команды.<br>
**@param** *Function* `fn` функци�?-генератор автодополнени�?,
    и�?полн�?ема�? в контек�?те текущей команды.
    Принимает параметры:<br>
        - *Object* `opts` параметры<br>
    Может возвращать проми�? или любое другое значение, ра�?�?матриваемое как результат и�?полнени�? команды.<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.helpful
Ставит флаг поддержки �?правки командной �?троки, т.е. вызов команды �? параметрами -h --help выводит �?правку по работе �? командой.<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.completable
Добавл�?ет поддержку автодополнени�? командной �?троки. Добавл�?ет�?�? подкоманда "completion", котора�? выполн�?ет в�?е необходимые дей�?тви�?.<br>
Может быть добавлен только дл�? главной команды.<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.usage
Возвращает тек�?т �?правки по и�?пользованию команды дл�? текущего �?кземпл�?ра.<br>
**@returns** *String* `usage` Тек�?т �?правки по и�?пользованию

#### Cmd.run
Разбирает аргументы из значени�?, возвращаемого NodeJS process.argv,
и запу�?кает текущую программу, т.е. вызывает process.exit по�?ле завершени�?
в�?ех дей�?твий.<br>
**@param** *Array* `argv`<br>
**@returns** *COA.Cmd* `this` �?кземпл�?р команды (дл�? поддержки цепочки методов)

#### Cmd.invoke
И�?полн�?ет переданную (или текущую) команду �? указанными параметрами и аргументами.<br>
**@param** *String|Array* `cmds`  подкоманда дл�? и�?полнени�? (необ�?зательно)<br>
**@param** *Object* `opts`  параметры, передаваемые команде (необ�?зательно)<br>
**@param** *Object* `args`  аргументы, передаваемые команде (необ�?зательно)<br>
**@returns** *Q.Promise*

#### Cmd.reject
Проваливает проми�?ы, возращенные в дей�?тви�?х.<br>
И�?пользует�?�? в .act() дл�? возврата �? ошибкой.<br>
**@param** *Object* `reason` причина провала<br>
    Вы можете определить метод toString() и �?вой�?тво toString()
    объекта причины провала.<br>
**@returns** *Q.promise* проваленный проми�?

#### Cmd.end
Завершает цепочку методов текущей подкоманды и возвращает �?кземпл�?р родитель�?кой команды.<br>
**@returns** *COA.Cmd* `parent` родитель�?ка�? команда

### Opt
Параметр — именованна�? �?ущно�?ть. У параметра может быть определено короткое или длинное им�? дл�? и�?пользовани�? из командной �?троки.<br>
**@namespace**<br>
**@class** Переданный параметр

#### Opt.name
Определ�?ет канониче�?кий идентификатор параметра, и�?пользуемый в вызовах API.<br>
**@param** *String* `_name` им�? параметра<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.title
Определ�?ет опи�?ание дл�? параметра, и�?пользуемое в тек�?товых �?ообщени�?х.<br>
**@param** *String* `_title` название параметра<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.short
�?азначает ключ дл�? короткого имени параметра, передаваемого из командной �?троки �? одинарным дефи�?ом (например, `-v`).<br>
**@param** *String* `_short`<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.long
�?азначает ключ дл�? длинного имени параметра, передаваемого из командной �?троки �? двойным дефи�?ом (например, `--version`).<br>
**@param** *String* `_long`<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.flag
Помечает параметр как логиче�?кий, т.е. параметр не имеющий значени�?.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.arr
Помечает параметр как принимающий множе�?твенные значени�?.<br>
Иначе будет и�?пользовано по�?леднее переданное значение параметра.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.req
Помечает параметр как об�?зательный.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.only
Интерпретирует параметр как команду,
т.е. программа будет завершена �?разу по�?ле выполнени�? параметра.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.val
�?азначает функцию валидации (или тран�?формации значени�?) дл�? значени�? параметра.<br>
Значение, полученное из командной �?троки, передает�?�? в функцию-валидатор прежде чем оно �?танет до�?тупно из API.<br>
И�?пользует�?�? дл�? валидации и тран�?формации введенных данных.<br>
**@param** *Function* `_val` функци�? валидации,
    и�?полн�?ема�? в контек�?те �?кземпл�?ра параметра
    и принимающа�? в каче�?тве един�?твенного параметра значение, полученное
    из командной �?троки<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.def
�?азначает значение параметра по умолчанию. Это значение также передает�?�?
в функцию валидации как обычное значение.<br>
**@param** *Object* `_def`<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.input
Помечает параметр как принимающий ввод пользовател�?. <br>
Позвол�?ет и�?пользовать валидацию дл�? STDIN.<br>
**@returns** *{COA.Opt}* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.output
Помечает параметр как вывод.<br>
Позвол�?ет и�?пользовать валидацию дл�? STDOUT.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.act
Добавл�?ет (или �?оздает) дей�?твие дл�? текущего параметра команды.
Это дей�?твие будет выполнено, е�?ли текущий параметр е�?ть
в �?пи�?ке полученных параметров (�? любым значением).<br>
**@param** *Function* `act` функци�?, выполн�?ема�? в контек�?те
    �?кземпл�?ра текущей команды и принимающа�? �?ледующие параметры:<br>
        - *Object* `opts` параметры команды<br>
        - *Array* `args` аргументы команды<br>
        - *Object* `res` объект-аккумул�?тор результатов<br>
    Функци�? может вернуть проваленный проми�? из Cmd.reject (в �?лучае ошибки)
    или любое другое значение, ра�?�?матриваемое как результат.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.comp
�?азначает ка�?томную функцию автодополнени�? дл�? текущей команды.<br>
**@param** *Function* `fn` функци�?-генератор автодоплнени�?, и�?полн�?ема�? в
    контек�?те �?кземпл�?ра команды.
    Принимает параметры:<br>
        - *Object* `opts` параметры автодополнени�?<br>
    Может возвращать проми�? или любое другое значение, ра�?�?матриваемое как результат и�?полнени�? команды.<br>
**@returns** *COA.Opt* `this` �?кземпл�?р параметра (дл�? поддержки цепочки методов)

#### Opt.end
Завершает цепочку методов текущего параметра и возвращает �?кземпл�?р родитель�?кой команды.<br>
**@returns** *COA.Cmd* `parent` родитель�?ка�? команда


### Arg
�?ргумент — неименованна�? �?ущно�?ть.<br>
�?ргументы передают�?�? из командной �?троки как �?пи�?ок неименованных значений.

#### Arg.name
Определ�?ет канониче�?кий идентификатор аргумента, и�?пользуемый в вызовах API.<br>
**@param** *String* `_name` им�? аргумента<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.title
Определ�?ет опи�?ание дл�? аргумента, и�?пользуемое в тек�?товых �?ообщени�?х.<br>
**@param** *String* `_title` опи�?ание аргумента<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.arr
Помечает аргумент как принимающий множе�?твенные значени�?.<br>
Иначе будет и�?пользовано по�?леднее переданное значение аргумента.<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.req
Помечает аргумент как об�?зательный.<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.val
�?азначает функцию валидации (или тран�?формации значени�?) дл�? аргумента.<br>
Значение, полученное из командной �?троки, передает�?�? в функцию-валидатор прежде чем оно �?танет до�?тупно из API.<br>
И�?пользует�?�? дл�? валидации и тран�?формации введенных данных.<br>
**@param** *Function* `_val` функци�? валидации,
    и�?полн�?ема�? в контек�?те �?кземпл�?ра аргумента
    и принимающа�? в каче�?тве един�?твенного параметра значение, полученное
    из командной �?троки<br>
**@returns** *COA.Opt* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.def
�?азначает дефолтное значение дл�? аргумента. Дефолтное значение передает�?�?
в функцию валидации как обычное значение.<br>
**@param** *Object* `_def`<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.output
Помечает параметр как вывод.<br>
Позвол�?ет назначить валидацию дл�? STDOUT.<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.comp
�?азначает ка�?томную функцию автодополнени�? дл�? текущего аргумента.<br>
**@param** *Function* `fn` функци�?-генератор автодоплнени�?,
    и�?полн�?ема�? в контек�?те текущей команды.
    Принимает параметры:<br>
        - *Object* `opts` параметры
Может возвращать проми�? или любое другое значение, ра�?�?матриваемое как результат и�?полнени�? команды.<br>
**@returns** *COA.Arg* `this` �?кземпл�?р аргумента (дл�? поддержки цепочки методов)

#### Arg.end
Завершает цепочку методов текущего аргумента и возвращает �?кземпл�?р родитель�?кой команды.<br>
**@returns** *COA.Cmd* `parent` родитель�?ка�? команда
